# Topic: Largest Among Three Numbers (Logical Operators in Functions)
def find_largest(a, b, c):
    if a >= b and a >= c:
        return a
    elif b >= a and b >= c:
        return b
    else:
        return c

print("Largest number is:", find_largest(15, 42, 23))
